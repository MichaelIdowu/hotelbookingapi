# This file exists only for tools that require a root 'Dockerfile'.
# The canonical Dockerfile lives at .container/.dockerfile
# Build with: docker build -f .container/.dockerfile -t hotelapi .
FROM scratch
